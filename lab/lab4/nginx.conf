worker_processes 1;

events { worker_connections 1024; }

http {
    upstream api_servers {
        server api1:5000;
        server api2:5000;
    }
    server {
        listen 80;
        location /api/ {
            proxy_pass http://api_servers;
        }
    }
}
